# control_logic_ls80_2_m_v01.c 部署與測試指南

## 📋 概述

`control_logic_ls80_2_m_v01.c` 是 LS80 壓力差控制邏輯的手動替換版本（v01），實現 **(P4-P2)→Pset** 追蹤控制。

### 主要特點

- **控制目標**：二次側壓力差（P4進水 - P2出水）
- **控制方式**：PID 自動控制 + 手動模式
- **泵速輸出**：簡化寫入方式（0-100% 直接寫入）
- **監控顯示**：P1 和 P3 壓力值
- **部署方式**：手動替換 control_logic_ls80_2.c 後編譯

---

## 🔧 與原版差異

| 項目 | 原版 (control_logic_ls80_2.c) | 新版 (control_logic_ls80_2_m_v01.c) |
|------|-------------------------------|-------------------------------------|
| 壓力差計算 | P1 - P3 (一次側) | P4 - P2 (二次側) |
| 感測器地址 | 411xxx (硬體地址) | 42xxx (映射地址) |
| 泵速寫入 | speed × 10 × 10 = mV | 直接寫入 0-100% |
| 參考架構 | 獨立實現 | 參照 ls80_3.c (流量控制) |
| 額外監控 | 無 | P1 和 P3 壓力顯示 |

---

## 📦 部署步驟

### 步驟 1：配置 JSON 感測器映射

在部署程式碼之前，**必須先配置壓力感測器映射**。

#### 使用 Redfish API 配置

```bash
curl -X POST "http://<device-ip>:8080/redfish/v1/ThermalEquipment/CDUs/1/Oem/Kenmec/Config.Write" \
  -H "Content-Type: application/json" \
  -d '{
    "AnalogInputCurrentConfigs": [
        {"board": 0, "channel": 0, "sensor_type": 1, "update_address": 2082, "name": "P1"},
        {"board": 0, "channel": 2, "sensor_type": 1, "update_address": 2083, "name": "P2"},
        {"board": 0, "channel": 1, "sensor_type": 1, "update_address": 2084, "name": "P3"},
        {"board": 0, "channel": 3, "sensor_type": 1, "update_address": 2085, "name": "P4"}
    ]
  }'
```

#### 驗證配置

```bash
curl -X GET "http://<device-ip>:8080/redfish/v1/ThermalEquipment/CDUs/1/Oem/Kenmec/Config.Read" | jq '.AnalogInputCurrentConfigs'
```

預期輸出：
```json
[
  {"board": 0, "channel": 0, "sensor_type": 1, "update_address": 2082, "name": "P1"},
  {"board": 0, "channel": 2, "sensor_type": 1, "update_address": 2083, "name": "P2"},
  {"board": 0, "channel": 1, "sensor_type": 1, "update_address": 2084, "name": "P3"},
  {"board": 0, "channel": 3, "sensor_type": 1, "update_address": 2085, "name": "P4"}
]
```

---

### 步驟 2：備份原始檔案

```bash
cd /home/stephen/dk-ipc/SDK_Kenmec_CDU/application_dexatek/kenmec/main_application/control_logic/ls80/

# 備份原始 control_logic_ls80_2.c
cp control_logic_ls80_2.c control_logic_ls80_2_original_backup.c

# 確認備份成功
ls -lh control_logic_ls80_2_original_backup.c
```

---

### 步驟 3：替換為新版本

```bash
# 將新版本改名為 control_logic_ls80_2.c
cp control_logic_ls80_2_m_v01.c control_logic_ls80_2.c

# 驗證替換成功
head -20 control_logic_ls80_2.c | grep "v01"
```

預期輸出：
```
 * control_logic_ls80_2_m_v01.c - LS80 壓力差控制邏輯（手動替換版本 v01）
```

---

### 步驟 4：編譯系統

```bash
cd /home/stephen/dk-ipc/SDK_Kenmec_CDU/application_dexatek/

# 執行編譯
./build_kenmec.sh
```

#### 檢查編譯結果

```bash
# 查看編譯輸出
echo "編譯狀態: $?"

# 如果成功，應該輸出 0
# 編譯狀態: 0
```

---

### 步驟 5：部署到設備

根據您的部署流程，將編譯後的二進位檔案部署到目標設備。

```bash
# 範例（根據實際環境調整）
scp build/output/kenmec_application root@<device-ip>:/usr/bin/
```

---

### 步驟 6：啟動並驗證

```bash
# SSH 連線到設備
ssh root@<device-ip>

# 重啟服務
systemctl restart kenmec_application

# 查看服務狀態
systemctl status kenmec_application

# 查看日誌（尋找 ls80_2_m_v01 標籤）
tail -f /var/log/kenmec_application.log | grep "ls80_2_m_v01"
```

預期日誌輸出：
```
[INFO] [ls80_2_m_v01] 初始化 LS80 壓力差控制邏輯 2 (手動版本 v01)
[INFO] [ls80_2_m_v01] === CDU壓力差控制系統執行 (v01) ===
[INFO] [ls80_2_m_v01] 壓力數據 - P1: 5.00, P2: 3.50, P3: 4.80, P4: 6.20 bar, 壓差(P4-P2): 2.70 bar
[INFO] [ls80_2_m_v01] 監控壓力 - P1(一次側進水): 5.00 bar, P3(一次側出水): 4.80 bar
```

---

## 🧪 測試指南

### 測試 1：啟用控制邏輯

```bash
# 使用 Modbus 工具或 Redfish API 啟用控制邏輯2
# 寫入 1 到 Register 41002

# 使用 modbus_client（如果可用）
modbus_client -a 41002 -v 1

# 或使用 Redfish API
curl -X POST "http://<device-ip>:8080/redfish/v1/..." \
  -H "Content-Type: application/json" \
  -d '{"RegisterAddress": 41002, "Value": 1}'
```

---

### 測試 2：設定目標壓差

```bash
# 設定目標壓差為 2.0 bar（寫入 20，因為精度是 0.1 bar）
# Register 45002

modbus_client -a 45002 -v 20

# 驗證設定
modbus_client -a 45002
# 預期輸出：20 (代表 2.0 bar)
```

---

### 測試 3：讀取壓力感測器數據

```bash
# 讀取 P1-P4 壓力（Register 42082-42085）
modbus_client -a 42082 -n 4

# 預期輸出範例（4個值，單位 0.01 bar）：
# 42082: 500  (P1 = 5.00 bar)
# 42083: 350  (P2 = 3.50 bar)
# 42084: 480  (P3 = 4.80 bar)
# 42085: 620  (P4 = 6.20 bar)
```

---

### 測試 4：驗證自動控制

```bash
# 確保泵浦在自動模式（Register 45021/45022 = 0）
modbus_client -a 45021 -v 0  # Pump1 自動模式
modbus_client -a 45022 -v 0  # Pump2 自動模式

# 查看日誌，確認 PID 控制運行
tail -f /var/log/kenmec_application.log | grep "PID"
```

預期日誌：
```
[DEBUG] [ls80_2_m_v01] 壓差PID - 誤差: 0.70, P: 1.40, I: 0.35, D: 0.07, 輸出: 45.2%
[INFO] [ls80_2_m_v01] 自動壓差控制完成 - PID輸出: 45.2%, 泵浦速度: Pump1=45.2%, Pump2=45.2%
```

---

### 測試 5：驗證泵速控制

```bash
# 讀取泵浦速度設定（Register 45015/45016）
modbus_client -a 45015  # Pump1 速度
modbus_client -a 45016  # Pump2 速度

# 預期輸出：45 (代表 45%)

# 讀取泵浦啟停狀態（Register 411101/411103）
modbus_client -a 411101  # Pump1 狀態
modbus_client -a 411103  # Pump2 狀態

# 預期輸出：1 (啟動), 0 (停止)
```

---

### 測試 6：手動模式測試

```bash
# 切換到手動模式
modbus_client -a 45021 -v 1  # Pump1 手動模式

# 手動設定泵速
modbus_client -a 45015 -v 60  # 設定 Pump1 速度為 60%

# 查看日誌，確認控制邏輯不干預
tail -f /var/log/kenmec_application.log | grep "手動模式"
```

預期日誌：
```
[INFO] [ls80_2_m_v01] 手動壓差控制模式 - 僅監控狀態
[DEBUG] [ls80_2_m_v01] 手動模式：等待操作員手動控制泵浦
```

---

## 📊 監控數據

### 壓力感測器數據（每秒更新）

| Register | 感測器 | 說明 | 精度 | 範圍 |
|----------|--------|------|------|------|
| 42082 | P1 | 一次側進水壓力 | 0.01 bar | 0-10 bar |
| 42083 | P2 | 二次側出水壓力 | 0.01 bar | 0-10 bar |
| 42084 | P3 | 一次側出水壓力 | 0.01 bar | 0-10 bar |
| 42085 | P4 | 二次側進水壓力 | 0.01 bar | 0-10 bar |

### 控制輸出

| Register | 說明 | 範圍 | 備註 |
|----------|------|------|------|
| 45015 | Pump1 速度 | 0-100% | 直接寫入百分比 |
| 45016 | Pump2 速度 | 0-100% | 直接寫入百分比 |
| 411101 | Pump1 啟停 | 0/1 | 0=停止, 1=啟動 |
| 411103 | Pump2 啟停 | 0/1 | 0=停止, 1=啟動 |

### 控制設定

| Register | 說明 | 精度 | 範圍 |
|----------|------|------|------|
| 45002 | 壓差設定值 | 0.1 bar | 0-10 bar |
| 45021 | Pump1 手動模式 | - | 0=自動, 1=手動 |
| 45022 | Pump2 手動模式 | - | 0=自動, 1=手動 |
| 41002 | 控制邏輯2 啟用 | - | 0=停用, 1=啟用 |

---

## 🔙 恢復原始版本

如果測試不理想，可以輕鬆恢復：

```bash
cd /home/stephen/dk-ipc/SDK_Kenmec_CDU/application_dexatek/kenmec/main_application/control_logic/ls80/

# 恢復原始版本
cp control_logic_ls80_2_original_backup.c control_logic_ls80_2.c

# 重新編譯
cd /home/stephen/dk-ipc/SDK_Kenmec_CDU/application_dexatek/
./build_kenmec.sh

# 重新部署（根據實際環境調整）
scp build/output/kenmec_application root@<device-ip>:/usr/bin/

# 重啟服務
ssh root@<device-ip>
systemctl restart kenmec_application
```

---

## 🐛 故障排除

### 問題 1：控制邏輯未啟動

**症狀**：日誌中看不到 `ls80_2_m_v01` 標籤

**解決方案**：
```bash
# 1. 檢查控制邏輯2是否啟用
modbus_client -a 41002
# 應該返回 1（啟用）

# 2. 如果是 0，手動啟用
modbus_client -a 41002 -v 1

# 3. 查看服務日誌
tail -f /var/log/kenmec_application.log | grep "ls80_2"
```

---

### 問題 2：壓力數據為 0

**症狀**：日誌顯示 `P1: 0.00, P2: 0.00, P3: 0.00, P4: 0.00 bar`

**解決方案**：
```bash
# 1. 檢查 JSON 配置是否正確
curl -X GET "http://<device-ip>:8080/.../Config.Read" | jq '.AnalogInputCurrentConfigs'

# 2. 驗證 Modbus 寄存器是否有數據
modbus_client -a 42082 -n 4

# 3. 如果全為 0，檢查：
#    - 硬體連接是否正常
#    - I/O 板是否通電
#    - 感測器是否正確連接到對應通道
```

---

### 問題 3：泵浦不啟動

**症狀**：PID 有輸出，但泵浦不運行

**解決方案**：
```bash
# 1. 檢查泵浦控制寄存器
modbus_client -a 411101  # Pump1 控制
modbus_client -a 411103  # Pump2 控制
# 應該返回 1（啟動）

# 2. 檢查泵浦速度設定
modbus_client -a 45015  # Pump1 速度
modbus_client -a 45016  # Pump2 速度
# 應該返回 > 0 的值

# 3. 檢查是否在手動模式
modbus_client -a 45021  # Pump1 手動模式
modbus_client -a 45022  # Pump2 手動模式
# 應該返回 0（自動模式）
```

---

### 問題 4：PID 控制不穩定

**症狀**：壓力差震盪，泵速頻繁變化

**解決方案**：
```bash
# 查看 PID 參數是否合適
# 當前參數：Kp=2.0, Ki=0.5, Kd=0.1

# 如果需要調整，需要修改程式碼並重新編譯
# 或等待未來版本支援動態參數調整
```

---

### 問題 5：編譯錯誤

**症狀**：`./build_kenmec.sh` 編譯失敗

**解決方案**：
```bash
# 1. 檢查是否正確替換檔案
ls -lh kenmec/main_application/control_logic/ls80/control_logic_ls80_2.c

# 2. 查看詳細編譯錯誤
./build_kenmec.sh 2>&1 | tee build.log
cat build.log | grep "error:"

# 3. 如果有編譯錯誤，恢復原始版本
cp kenmec/main_application/control_logic/ls80/control_logic_ls80_2_original_backup.c \
   kenmec/main_application/control_logic/ls80/control_logic_ls80_2.c
```

---

## 📈 效能基準

### 預期控制效能

- **穩態誤差**：< 0.1 bar
- **響應時間**：< 10 秒（從偏差到穩定）
- **超調量**：< 20%
- **更新頻率**：1 Hz（每秒執行一次）

### 記憶體使用

- **程式碼大小**：~3 KB
- **靜態記憶體**：~1 KB（PID controller + sensor data）
- **堆疊使用**：~2 KB（每次執行）

---

## 📝 版本歷史

### v01 (2025)
- 初始版本
- 實現 (P4-P2)→Pset 壓力差追蹤控制
- 使用 42xxx 映射地址
- 簡化泵速寫入方式（0-100% 直接寫入）
- 參照 ls80_3.c 流量控制架構
- PID 參數：Kp=2.0, Ki=0.5, Kd=0.1
- 暫不實施安全機制

---

## 📞 聯絡資訊

如有問題或建議，請聯絡：
- 開發者：Claude AI
- 基於架構：control_logic_ls80_3.c（流量控制）
- 文檔版本：v01

---

## ⚠️ 注意事項

1. **備份重要**：在替換檔案前，務必備份原始版本
2. **JSON 配置必需**：必須先配置感測器映射，否則壓力數據為 0
3. **測試環境**：建議先在測試環境驗證，再部署到生產環境
4. **監控日誌**：部署後持續監控日誌，確保控制邏輯正常運行
5. **安全機制**：本版本暫不包含壓力上下限保護，請注意監控
6. **手動恢復**：如有問題，可隨時恢復原始版本

---

**祝部署順利！**
